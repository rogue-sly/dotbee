[package]
name = "utils"
version = "0.1.0"
edition = "2024"

[dependencies]
indexmap = { workspace = true }
dirs = { workspace = true }
nix = { workspace = true }
config = { path = "../config/" }
context = { path = "../context/" }
