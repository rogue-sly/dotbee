#!/usr/bin/env bash
set -euo pipefail

source "$(dirname "$0")/../_lib.sh"
init
validate_version

step "Generating checksums"

(cd dist && sha256sum bin/* deb/* rpm/* | sed -E 's#^([0-9a-f]{64})  [^/]+/#\1  #' >checksums.txt)
ok "checksums.txt generated"
